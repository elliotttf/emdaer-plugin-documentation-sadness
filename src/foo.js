module.exports = {
  /**
   * I don't really want to document this file becuase it's not directly
   * exported, but here we are!
   *
   * @return {undefined}
   */
  foo()  {},

  /**
   * @private
   * You won't see this documentation.
   */
  bar() {},
};

